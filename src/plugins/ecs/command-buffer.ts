/**
 * @file ecs plugin — deferred structural-op command buffer.
 */

/**
 * Creates the command buffer that defers structural ops (spawn/despawn/add/remove) to a flush boundary.
 *
 * @example
 * ```ts
 * const buffer = createCommandBuffer();
 * ```
 */
export function createCommandBuffer(): unknown {
  throw new Error("not implemented");
}
