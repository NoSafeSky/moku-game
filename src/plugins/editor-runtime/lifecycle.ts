/**
 * @file editor-runtime plugin — onStart lifecycle skeleton.
 */

/**
 * Deps-ready wiring: apply the initial author-mode stage gate and flip `started`.
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @throws {Error} Always in the skeleton — implemented during build.
 * @example
 * ```ts
 * onStart: start;
 * ```
 */
export function start(_ctx: unknown): void {
  throw new Error("not implemented");
}
