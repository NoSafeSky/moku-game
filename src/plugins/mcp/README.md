# mcp

> Complex plugin — first-class MCP server exposing the whole runtime to agent clients over stdio + Streamable HTTP.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
