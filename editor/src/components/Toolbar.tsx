/** @file Toolbar panel — the toolbar island renders undo/redo/play/stop/step/save/load actions here. */
export function Toolbar() {
  return (
    <section data-island="toolbar" data-panel="toolbar" aria-label="Toolbar">
      {/* toolbar island renders data-action buttons here */}
    </section>
  );
}
