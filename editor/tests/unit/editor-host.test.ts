import { describe, it } from "vitest";

describe("editor-host", () => {
  it.todo("boots the game app and mounts the canvas into the viewport (W1)");
  it.todo("polls bridge.snapshot() and notifies onSnapshot subscribers (W1)");
  it.todo("getEditor() throws before startEditor() has resolved (W1)");
});
