# renderer

> Complex plugin — PixiJS v8 rendering — owns the GPU Application (onStart/onStop) and the transform sync stage system.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
