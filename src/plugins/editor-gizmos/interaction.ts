/**
 * @file editor-gizmos plugin — federated pointer-event drag pipeline skeleton.
 */

/**
 * Wires the federated pointer-event drag pipeline (pointerdown → move → up → commit) onto the handle.
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @throws {Error} Always in the skeleton — implemented during build.
 * @example
 * ```ts
 * attachInteraction(ctx);
 * ```
 */
export function attachInteraction(_ctx: unknown): void {
  throw new Error("not implemented");
}
