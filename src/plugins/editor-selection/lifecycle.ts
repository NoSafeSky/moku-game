/**
 * @file editor-selection plugin — onStart lifecycle skeleton.
 */

/**
 * Deps-ready wiring: capture the ecs/renderer/camera/input APIs, leaving the plugin disabled.
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @throws {Error} Always in the skeleton — implemented during build.
 * @example
 * ```ts
 * onStart: start;
 * ```
 */
export function start(_ctx: unknown): void {
  throw new Error("not implemented");
}
