/**
 * @file component-registry plugin — type-level contracts (populated during build).
 *
 * Placeholder: the build wave fills in the expectTypeOf / `@ts-expect-error` assertions from the
 * spec's "Type-level tests" section (six-member ComponentCategory, readonly entry fields, etc.).
 */
export {};
