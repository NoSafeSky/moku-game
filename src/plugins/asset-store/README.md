# asset-store

> [Standard] plugin — IndexedDB store for imported image assets (blobs + session blob: URLs; never serialized).

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
