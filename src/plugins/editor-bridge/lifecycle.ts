/**
 * @file editor-bridge plugin — onStart lifecycle skeleton.
 */

/**
 * Deps-ready wiring: install the reflection↔commands validator seam and the gizmo gesture sink; probe mcp.
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @throws {Error} Always in the skeleton — implemented during build.
 * @example
 * ```ts
 * onStart: start;
 * ```
 */
export function start(_ctx: unknown): void {
  throw new Error("not implemented");
}
