/**
 * @file editor-gizmos plugin — onStart lifecycle skeleton.
 */

/**
 * Deps-ready wiring: capture deps, build the overlay + handle on the renderer stage, wire interaction.
 *
 * @param _ctx - Plugin context (unused in skeleton).
 * @throws {Error} Always in the skeleton — implemented during build.
 * @example
 * ```ts
 * onStart: start;
 * ```
 */
export function start(_ctx: unknown): void {
  throw new Error("not implemented");
}
