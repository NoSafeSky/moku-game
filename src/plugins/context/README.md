# context

> Standard plugin — binds the framework well-known resources (Assets, GameContext) onto the ECS world so systems reach them via `world.resource(token)`.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
