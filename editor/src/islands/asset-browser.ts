/** @file Asset-browser island — getEditor().assets.entries() → asset rows. */
import { createIsland } from "@moku-labs/web/browser";

/**
 * Asset-browser island — stub. W3 wires getEditor().assets.entries() into a browsable list.
 */
export const assetBrowser = createIsland("asset-browser", {
  onMount() {
    throw new Error("[asset-browser] not implemented");
  }
});
