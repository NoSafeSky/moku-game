/**
 * @file hierarchy plugin — type-level contracts (populated during build).
 *
 * Placeholder: the build wave fills in the expectTypeOf / `@ts-expect-error` assertions from the
 * spec's "Type-level tests" section (Node is Component<NodeValue>; worldOf returns TransformValue;
 * childrenOf/roots are readonly; no method carries a type parameter).
 */
export {};
