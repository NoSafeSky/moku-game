import { describe, it } from "vitest";

describe("field-controls", () => {
  it.todo("renders an editable control for each FieldDescriptor kind (W2)");
  it.todo("reads the value back out of the control for each kind (W2)");
  it.todo("renders readonly fields as disabled controls (W2)");
});
