/**
 * @file ecs plugin — typed query construction over the archetype store.
 */

/**
 * Creates a typed query that matches entities holding the requested components.
 *
 * @example
 * ```ts
 * const q = createQuery();
 * ```
 */
export function createQuery(): unknown {
  throw new Error("not implemented");
}
