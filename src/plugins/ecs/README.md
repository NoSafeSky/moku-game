# ecs

> Complex plugin — ECS data/runtime core: generational entities, archetype object-SoA storage, typed queries, deferred command buffer, world.tick(dt).

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
