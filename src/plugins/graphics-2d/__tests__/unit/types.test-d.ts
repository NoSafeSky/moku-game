/**
 * @file graphics-2d plugin — type-level contracts (populated during build).
 *
 * Placeholder: the build wave fills in the expectTypeOf / `@ts-expect-error` assertions from the
 * spec's "Type-level tests" section (SpriteRendererValue/ShapeValue field types; the two token
 * getters; no explicit generics on createPlugin; no Pixi type on the api surface).
 */
export {};
